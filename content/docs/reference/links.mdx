---
title: Links
description: Every outbound link referenced across these docs, rendered from a single registry.
---

# Links

Every outbound URL referenced across these docs lives in one registry -
`docs-platform/links.ts` - and this page renders straight from it. A URL that
needs updating is edited exactly once, and the drift checker can verify the
registry rather than grepping prose.

<LinkIndex />
